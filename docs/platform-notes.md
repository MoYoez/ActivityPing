# Platform Notes

- **Windows**: foreground app capture and system media data use native Windows APIs.
- **macOS**: window title capture may require Accessibility permission.
- **Linux**: support depends on desktop environment and available tools such as `playerctl`, `xprop`, or KDE/GNOME-specific probes.

The built-in self-test helps verify what is available on the current machine.

